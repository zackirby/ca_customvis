import IRSColor from "@waca/vipr-rs-v1/com/ibm/vipr/rs/v1/IRSColor";
import { Color } from "./Color";
/**
 * Internal class which converts a Color instance to an IRSColor instance.
 * @ignore
 */
export declare class _RSColor extends Color implements IRSColor {
    readonly _color: Color;
    constructor(_color: Color);
    getRed(): number;
    getGreen(): number;
    getBlue(): number;
    getAlpha(): number;
}
